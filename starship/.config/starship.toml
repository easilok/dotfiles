[aws]
disabled=true

[custom.production_env]
command =  '~/.local/bin/detect_production_env.sh'
when = '[ -f ~/.local/bin/detect_production_env.sh ]'
style = 'bold red'
format = '[${output} ]($style)'
description = 'Show danger indication when a production environment variable is found.'
